/*
 * This file defines ExampleUnitTest, a test class used to verify Scene app behavior.
 * It contains automated test coverage for ExampleUnit behavior and expected results.
 * Its functions include addition_isCorrect to load data, handle user actions, validate input, and save results.
 * It connects this feature to the Scene app's UI, data, navigation, and verification flow.
 */

package com.example.seprojectpart3;

import org.junit.Test;

import static org.junit.Assert.*;






public class ExampleUnitTest {
    @Test
    public void addition_isCorrect() {
        assertEquals(4, 2 + 2);
    }
}
